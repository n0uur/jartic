package Shared.Model;

public class GameServer {
    public static enum gameStatus {
        GAME_WAITING,
        GAME_STARTING,
        GAME_PLAYING,
        GAME_WAITING_WORD,
        GAME_ENDED_ROUND,
        GAME_ENDED,
    }
}
